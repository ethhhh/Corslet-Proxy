{
  "name": "corslet",
  "version": "1.0.0",
  "description": "CORS proxy using Cloudflare Workers",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler publish",
    "deploy:prod": "wrangler publish --env production"
  },
  "keywords": ["cors", "proxy", "cloudflare"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
